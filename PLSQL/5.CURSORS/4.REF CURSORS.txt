SET SERVEROUTPUT ON;
/
DECLARE 
TYPE C1_SREF IS REF CURSOR RETURN EMP%ROWTYPE;
MRC1SREF C1_SREF;
REC_VAR EMP%ROWTYPE;
BEGIN
OPEN MRC1SREF FOR SELECT * FROM EMP WHERE EMPNO=7900;
FETCH MRC1SREF INTO REC_VAR;
DBMS_OUTPUT.PUT_LINE('EMPLOYEE NAME'||'='||REC_VAR.ENAME||','||'EMPLOYEE SALARY'||'='||REC_VAR.SAL);
CLOSE MRC1SREF;
END;
/
 
